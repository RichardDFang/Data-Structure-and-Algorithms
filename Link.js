class Link {
    constructor() {
        this.node = {
            index: null,
            next: null,
            size: 0
        };
    }
    insert(index, data) {
        while(this.node.index && this.node.index >= 0) {
            
        }
    }
}

export default Link;